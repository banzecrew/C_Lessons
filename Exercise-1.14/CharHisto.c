#include <stdio.h>

#define MAX_LEN 10

int main(int argc, char const *argv[])
{
	int c;
	int i;
	int symbols[MAX_LEN + 1];

	for (i = 0; i <= MAX_LEN; ++i)
		symbols[i] = 0;

	while ((c = getchar()) != EOF) {
		
	}

	return 0;
}